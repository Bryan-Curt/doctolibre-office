import { PractitionerPipe } from './practitioner.pipe';

describe('PractitionerPipe', () => {
  it('create an instance', () => {
    const pipe = new PractitionerPipe();
    expect(pipe).toBeTruthy();
  });
});
